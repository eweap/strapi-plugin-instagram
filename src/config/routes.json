{
    "routes": [
        {
            "method": "GET",
            "path": "/connect",
            "handler": "InstagramConnect.getAuthorizationUrl",
            "config": {
                "policies": []
            }
        },
        {
            "method": "POST",
            "path": "/callback",
            "handler": "InstagramConnect.handleCallback",
            "config": {
                "policies": []
            }
        },

        {
            "method": "GET",
            "path": "/posts",
            "handler": "InstagramPosts.index",
            "config": {
                "policies": []
            }
        }
    ]
}
